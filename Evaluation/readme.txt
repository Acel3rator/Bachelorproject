Zur Dateibenennung:

<botname>_game_<welches spiel (0-9)>_level_<welches level (0-4)>_<wievielter anlauf>.txt

im moment werden nur die spiele der CEEC validation gespielt.
In den einzelnen txts findest du dann:

<gametick>   <gamescore>   <action>   <winner>

wobei winner glaub ich noch nicht so wirklich funktioniert, uns kommts aber ja eh eher auf den gamescore an.